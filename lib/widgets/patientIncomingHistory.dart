import 'package:flutter/material.dart';

class PatientIcomingHistory extends StatelessWidget {
  const PatientIcomingHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
